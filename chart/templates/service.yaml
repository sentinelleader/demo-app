apiVersion: v1
kind: Service
metadata:
  name: deepak-demo-app
  labels:
    app: webapp
  namespace: dk
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: webapp
